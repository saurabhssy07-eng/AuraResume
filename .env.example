# Database Configuration
# Leave MONGODB_URI empty to automatically fall back to local JSON database storage (data/local_db.json)
MONGODB_URI=
JWT_SECRET=super_secret_jwt_key_change_me_in_production
NODE_ENV=development
